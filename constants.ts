
import { Momento, MustWin, ImportanciaPartida, Desfalques, Tendencia, SituacaoCasaFora, ValorPotencial, EstadoEmocional, MotivacaoEntrada, AutoAvaliacao } from './types';

export const MOMENTO_OPTIONS = Object.values(Momento);
export const MUST_WIN_OPTIONS = Object.values(MustWin);
export const IMPORTANCIA_PARTIDA_OPTIONS = Object.values(ImportanciaPartida);
export const DESFALQUES_OPTIONS = Object.values(Desfalques);
export const TENDENCIA_OPTIONS = Object.values(Tendencia);
export const SITUACAO_CASA_FORA_OPTIONS = Object.values(SituacaoCasaFora);
export const VALOR_POTENCIAL_OPTIONS = Object.values(ValorPotencial);
export const ESTADO_EMOCIONAL_OPTIONS = Object.values(EstadoEmocional);
export const MOTIVACAO_ENTRADA_OPTIONS = Object.values(MotivacaoEntrada);
export const AUTOAVALIACAO_OPTIONS = Object.values(AutoAvaliacao);
